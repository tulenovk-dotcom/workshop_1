"""Оповещения администратора о событиях сайта.

Пока здесь только заготовка: отправку в Telegram делаем отдельным шагом.
Важное правило для всех функций модуля — сбой оповещения не должен мешать
основному действию пользователя. Вызывающий код оборачивает вызов в
try/except, а сама функция ничего не возвращает.
"""

import sqlite3


def notify_new_application(application: sqlite3.Row | dict) -> None:
    """Сообщить администратору о новой заявке на размещение.

    Реализации пока нет. Когда появится отправка в Telegram, сюда придут
    токен бота и идентификатор чата из переменных окружения, а сам вызов
    останется прежним.
    """
